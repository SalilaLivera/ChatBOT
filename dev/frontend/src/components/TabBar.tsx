import { Pressable, StyleSheet, Text, View } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { colors, fonts } from "@/theme";
const items = [{ route: "home", label: "Home", icon: "home-outline" as const, active: "home" as const }, { route: "profile", label: "Profile", icon: "person-outline" as const, active: "person" as const }, { route: "insights", label: "Insights", icon: "bar-chart-outline" as const, active: "bar-chart" as const }, { route: "settings", label: "Settings", icon: "settings-outline" as const, active: "settings" as const }];
export function TabBar({ state, navigation }: any) { const activeRoute = state.routes[state.index]?.name; return <View style={styles.shell}><View style={styles.bar}>{items.map(item => { const focused = activeRoute === item.route; return <Pressable key={item.route} accessibilityRole="button" accessibilityLabel={item.label} onPress={() => navigation.navigate(item.route)} style={styles.item}><Ionicons name={focused ? item.active : item.icon} size={19} color={focused ? colors.deepPink : colors.muted} /><Text style={[styles.label, focused && styles.activeLabel]}>{item.label}</Text></Pressable>; })}</View>{activeRoute !== "chat" && <Pressable accessibilityRole="button" accessibilityLabel="Open chat" onPress={() => navigation.navigate("chat", { consent: "1" })} style={styles.fab}><Ionicons name="chatbubble-ellipses" size={23} color={colors.white} /></Pressable>}</View>; }
const styles = StyleSheet.create({ shell: { height: 72, backgroundColor: colors.card, borderTopWidth: 1, borderTopColor: colors.border, position: "relative", shadowColor: colors.primary, shadowOpacity: 0.08, shadowRadius: 8, shadowOffset: { width: 0, height: -3 }, elevation: 2, boxShadow: "0 -3px 8px rgba(217,108,138,.08)" as any }, bar: { flex: 1, flexDirection: "row", justifyContent: "space-around", alignItems: "center", paddingHorizontal: 8 }, item: { minWidth: 64, minHeight: 58, alignItems: "center", justifyContent: "center", gap: 3 }, label: { color: colors.muted, fontFamily: fonts.bold, fontSize: 10 }, activeLabel: { color: colors.deepPink }, fab: { position: "absolute", right: 28, top: -69, width: 58, height: 58, borderRadius: 29, backgroundColor: colors.primary, alignItems: "center", justifyContent: "center", borderWidth: 5, borderColor: colors.card, shadowColor: colors.deepPink, shadowOpacity: 0.25, shadowRadius: 9, elevation: 6 } });
